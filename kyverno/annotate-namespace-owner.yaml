apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: annotate-namespace-owner
spec:
  rules:
  - name: add-owner-annotation
    match:
      resources:
        kinds:
        - Namespace
    mutate:
      patchStrategicMerge:
        metadata:
          annotations:
            owner: "crew"